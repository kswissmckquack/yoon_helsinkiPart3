{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook part 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "test",
    "build:ui": "sudo rm -rf build && cd ../part2/2.phonebook/ && npm run build --prod && cp -r build ../../part3/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "phonebook"
  },
  "keywords": [
    "phonebook"
  ],
  "author": "Kevin Yoon",
  "license": "UNLICENSED",
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.12",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  },
  "proxy": "http://localhost:3001"
}
